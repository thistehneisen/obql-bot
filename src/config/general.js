const nickname = 'obql';
const selfCheck = 5000;
const commandPrefix = '!';
var commands = [];
var months = ["January", "February", "March", "April", "May", "June",
    "July", "August", "September", "October", "November", "December"];

module.exports = {
    nickname,
    commandPrefix,
    selfCheck,
    commands,
    months
}
